<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Route</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="page">
        <header class="header">
            <div>
                <p class="eyebrow">Edit Chat</p>
                <h2>Edit message</h2>
                <p class="subtle">This chat was sent from <strong><%= chat.from %></strong> to <strong><%= chat.to %></strong>.</p>
            </div>
            <a class="btn btn-ghost" href="/chats">Back</a>
        </header>

        <form class="card" method="post" action="/chats/<%=chat._id%>?_method=put">
            <label class="field">
                <span>Message</span>
                <textarea name="msg" rows="8" required><%= chat.msg %></textarea>
            </label>
            <button class="btn btn-primary" type="submit">Save</button>
        </form>
    </div>
           
</body>
</html>